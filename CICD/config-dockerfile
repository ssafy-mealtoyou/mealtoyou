FROM amazoncorretto:17-alpine
WORKDIR /app
ENV EUREKA_NAME='admin' \
    VAULT_HOST='' \
    VAULT_TOKEN='' \
    VAULT_URI='' \
    EUREKA_PASSWORD='' \
    BACKEND='' \
    BROKER='kafka:9092'

COPY libs/config-0.0.1-SNAPSHOT.jar /app/app.jar
CMD ["java", "-jar", "app.jar"]
